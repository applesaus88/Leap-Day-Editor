# Runtime deps for the standalone level-editor patcher.
# TypeTreeGeneratorAPI is required: modbuild generates a Level/MonoBehaviour type
# tree for any mod carrying custom levels (allow-all-elements, ordered lists,
# firebars, element overrides, flag-checkpoints). Without it the frozen app
# crashes on import (ModuleNotFoundError: No module named 'TypeTreeGeneratorAPI').
UnityPy==1.25.0
TypeTreeGeneratorAPI==0.0.10
lief==0.17.6
pyinstaller==6.20.0
